import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFEA2831);
  static const Color background = Colors.black;
  static const Color surface = Color(0xFF1A1A1A);
  static const Color success = Color(0xFF4CAF50);
  static final Color border = Colors.white.withOpacity(0.1);
  static final Color textHint = Colors.white.withOpacity(0.4);

  static const Color successToast = Color(0xFF00C853);
  static const Color infoToast = Color(0xFF2979FF);
  static const Color errorToast = Color(0xFFD50000);
}